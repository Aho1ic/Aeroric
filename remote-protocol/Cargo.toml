[package]
name = "remote-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared relay control-plane types for Aeroric desktop and remote-relay"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
