import type React from "react";

export const usage = {
  usageDashboard: {
    flex: 1,
    minWidth: 0,
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
    background: "var(--bg-panel)",
  },
  usageDashboardHeader: {
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "space-between",
    gap: 18,
    padding: "18px 22px 14px",
    borderBottom: "1px solid var(--border-dim)",
    flexShrink: 0,
  },
  usageDashboardTitle: {
    margin: 0,
    color: "var(--text-primary)",
    fontSize: 18,
    fontWeight: 720,
    lineHeight: 1.3,
  },
  usageDashboardRange: {
    marginTop: 4,
    color: "var(--text-muted)",
    fontSize: 11.5,
    lineHeight: 1.4,
    whiteSpace: "nowrap",
    flexShrink: 0,
  },
  usageDashboardControls: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-end",
    flexWrap: "wrap",
    gap: 8,
  },
  usageHeaderRight: {
    minWidth: 0,
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-end",
    flexWrap: "wrap",
    gap: 12,
  },
  usageLiveStatus: {
    display: "inline-flex",
    alignItems: "center",
    gap: 8,
    minHeight: 28,
    padding: "4px 9px",
    border: "1px solid var(--border-dim)",
    borderRadius: "var(--radius-md)",
    background: "var(--bg-subtle)",
  },
  usageLiveText: {
    display: "flex",
    flexDirection: "column",
    gap: 1,
    color: "var(--text-hint)",
    fontSize: 9.5,
    lineHeight: 1.25,
    whiteSpace: "nowrap",
  },
  usageSegment: {
    display: "inline-flex",
    alignItems: "center",
    gap: 3,
    padding: 3,
    border: "1px solid var(--border-dim)",
    borderRadius: "var(--radius-md)",
    background: "var(--bg-subtle)",
  },
  usageDashboardScroll: {
    flex: 1,
    minHeight: 0,
    overflow: "auto",
    padding: "18px 22px 28px",
  },
  usageMetricGrid: {
    display: "grid",
    gap: 10,
  },
  usageMetricCard: {
    minWidth: 0,
    minHeight: 96,
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-between",
    padding: "13px 14px",
    border: "1px solid var(--border-dim)",
    borderRadius: "var(--radius-md)",
    background: "var(--bg-card)",
    boxShadow: "var(--shadow-sm)",
  },
  usageMetricHead: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 8,
  },
  usageMetricLabel: {
    minWidth: 0,
    color: "var(--text-muted)",
    fontSize: 11.5,
    fontWeight: 600,
    lineHeight: 1.3,
  },
  usageMetricIcon: {
    width: 26,
    height: 26,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
    borderRadius: "var(--radius-sm)",
    background: "var(--bg-subtle)",
    color: "var(--text-muted)",
  },
  usageMetricValue: {
    marginTop: 11,
    color: "var(--text-primary)",
    fontFamily: "var(--font-mono)",
    fontWeight: 720,
    lineHeight: 1.2,
    whiteSpace: "nowrap",
  },
  usageChartSection: {
    marginTop: 14,
    padding: 16,
    border: "1px solid var(--border-dim)",
    borderRadius: "var(--radius-md)",
    background: "var(--bg-card)",
    boxShadow: "var(--shadow-sm)",
  },
  usageSectionHead: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 14,
    marginBottom: 16,
  },
  usageSectionTitle: {
    color: "var(--text-primary)",
    fontSize: 13,
    fontWeight: 700,
  },
  usageLegend: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-end",
    flexWrap: "wrap",
    gap: 10,
  },
  usageLegendItem: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    color: "var(--text-muted)",
    fontSize: 10.5,
  },
  usageLegendSwatch: {
    width: 8,
    height: 8,
    borderRadius: 2,
    flexShrink: 0,
  },
  usageChartViewport: {
    position: "relative",
    overflowX: "auto",
    overflowY: "hidden",
    padding: "2px 2px 4px",
  },
  usageChart: {
    height: 218,
    display: "flex",
    alignItems: "stretch",
    gap: 8,
  },
  usageChartAxis: {
    width: 88,
    flexShrink: 0,
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-between",
    paddingBottom: 24,
    color: "var(--text-hint)",
    fontFamily: "var(--font-mono)",
    fontSize: 9.5,
    textAlign: "right",
  },
  usageChartPlot: {
    flex: 1,
    minWidth: 0,
    display: "flex",
    alignItems: "stretch",
    gap: 10,
    padding: "0 12px",
    borderBottom: "1px solid var(--border-medium)",
    backgroundImage: "linear-gradient(to bottom, var(--border-dim) 1px, transparent 1px)",
    backgroundSize: "100% 25%",
  },
  usageChartColumn: {
    minWidth: 14,
    flex: "1 1 0",
    display: "flex",
    flexDirection: "column",
    justifyContent: "flex-end",
    alignItems: "center",
    position: "relative",
    outline: "none",
  },
  usageChartBar: {
    minHeight: 2,
    alignSelf: "center",
    display: "flex",
    flexDirection: "column-reverse",
    overflow: "hidden",
    borderRadius: "5px 5px 2px 2px",
    background: "var(--bg-subtle)",
    boxShadow: "inset 0 0 0 1px color-mix(in srgb, var(--border-dim) 65%, transparent)",
  },
  usageChartLabel: {
    height: 24,
    paddingTop: 6,
    overflow: "visible",
    color: "var(--text-hint)",
    fontFamily: "var(--font-mono)",
    fontSize: 9,
    lineHeight: 1,
    textAlign: "center",
    whiteSpace: "nowrap",
  },
  usageChartTooltip: {
    position: "absolute",
    top: 8,
    right: 10,
    width: 210,
    display: "flex",
    flexDirection: "column",
    gap: 7,
    padding: "10px 11px",
    border: "1px solid var(--border-medium)",
    borderRadius: "var(--radius-md)",
    background: "color-mix(in srgb, var(--bg-card) 94%, transparent)",
    boxShadow: "var(--shadow-md)",
    color: "var(--text-secondary)",
    fontSize: 10.5,
    pointerEvents: "none",
  },
  usageChartTooltipHead: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 12,
    paddingBottom: 7,
    borderBottom: "1px solid var(--border-dim)",
    color: "var(--text-primary)",
    fontFamily: "var(--font-mono)",
  },
  usageChartTooltipRow: {
    display: "grid",
    gridTemplateColumns: "8px minmax(0, 1fr) auto",
    alignItems: "center",
    gap: 7,
  },
  usageChartTooltipTotal: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 12,
    paddingTop: 7,
    borderTop: "1px solid var(--border-dim)",
    color: "var(--text-primary)",
    fontFamily: "var(--font-mono)",
  },
  usageCostNote: {
    marginTop: 14,
    display: "flex",
    alignItems: "flex-start",
    gap: 7,
    color: "var(--text-muted)",
    fontSize: 10.5,
    lineHeight: 1.5,
  },
  usageState: {
    minHeight: 280,
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    gap: 10,
    padding: 24,
    color: "var(--text-muted)",
    fontSize: 12,
    textAlign: "center",
  },
  usageSourceSummary: {
    marginTop: 14,
    display: "flex",
    flexDirection: "column",
    gap: 14,
    padding: 16,
    border: "1px solid var(--border-dim)",
    borderRadius: "var(--radius-md)",
    background: "var(--bg-card)",
    boxShadow: "var(--shadow-sm)",
  },
  usageSourceList: {
    display: "grid",
    gap: 12,
  },
  usageSourceRow: {
    minWidth: 0,
    display: "grid",
    gridTemplateColumns: "80px minmax(110px, 1fr) auto auto",
    alignItems: "center",
    gap: 12,
    color: "var(--text-secondary)",
    fontSize: 11.5,
  },
  usageSourceTrack: {
    height: 8,
    overflow: "hidden",
    borderRadius: 4,
    background: "var(--bg-subtle)",
  },
  usageSourceFill: {
    height: "100%",
    minWidth: 2,
    borderRadius: 4,
  },
  usageSourceValue: {
    color: "var(--text-muted)",
    fontFamily: "var(--font-mono)",
    fontSize: 10.5,
    textAlign: "right",
    whiteSpace: "nowrap",
  },
} satisfies Record<string, React.CSSProperties>;
